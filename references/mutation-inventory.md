# 變異盤點：測試自己有沒有在守東西

> 這份文件是**規格**，`scripts/mutations.yaml`（清單）與 `scripts/mutate.py`（跑法）
> 是它的實作。三者不一致時以本檔為準，**先改本檔再改碼**（紅線 9）。

## 為什麼要有這一層

`health-alarms.md` 講的是「警報自己把自己關掉」。這一份講的是再上面一層：
**測試自己有沒有在守東西**。

2026-07-26 當天 selftest 是 224 條。224 是一個很好看的數字，而它回答不了唯一重要的
那個問題：**把碼改壞，會不會有東西紅？** 同日實測，把 `pulse-monitor.py` 的 `main()`
結尾 `return rc` 改成 `return 0`——整條鏈紅不紅就靠這個數字——selftest **224/224
全過**。224 條測試，一條都沒動。

（「幾條」這個數字本身不必再追。它會隨著補測試一直長，而它長多少跟守不守得住
沒有關係——那正是這一節在講的事。要看守不守得住，跑 `python3 scripts/mutate.py`。）

這是同一個病的第五個實例：**用「測試有幾條」代理「壞掉會不會被抓到」**。
兩者在正常路徑上正相關，所以平常看不出來；分岔的那天，正好就是碼被改壞的那天。

量「壞掉會不會被抓到」只有一個誠實的方法：**真的把碼改壞，看有沒有紅**。

## 為什麼不能用手做

2026-07-26 之前做過一輪手工的：一條一條 `sed` 進去、跑 selftest、看幾條紅、再還原。
結論是「94 個注入點裡 47 個存活」。那句話寫進 BACKLOG 的當天就開始腐爛——它是
**174 條測試**時量的，一週後 selftest 長到 224 條，那個比例已經沒有任何意義，
但它還印在清單上，看起來像個現況。

手工盤點的產物是**一個數字**；數字會過期，而過期不會讓任何東西變紅。
可重跑的產物是**一個判準**；判準會自己更新。

手工那輪還踩到兩個坑，兩個都必須寫進工具裡，否則工具會重蹈覆轍：

### 坑一：針腳不唯一，會假裝成「存活」

needle `'        if r.get("unenriched_undated"):'` 在 `pulse-monitor.py` 裡命中兩處
（console print 區塊與 `[alert]` 區塊都是 8 格縮排）。`str.replace(a, b, 1)` 改到了
前者，於是 alert 那條路徑根本沒被動過，selftest 全綠——差一點就得出「這個釘子
不存在」的結論，然後去補一條早就存在的測試。

規則：**注入前先 `assert 出現次數 == 1`**。次數不是 1 的條目一律判為
**stale（清單過期）**，不是「通過」。stale 要跟 survivor 一樣讓工具回非零——
一份自己悄悄跳過條目的清單，就是一顆永遠綠的燈。

### 坑二：改壞語法不算變異

一個讓 `render()` 直接 raise 的改動會讓 selftest 紅，但那紅的是崩潰、不是斷言。
把它算成「殺掉了」，等於用「碼能不能跑」代理「碼對不對」——又一個比事實寬鬆的
代理指標。

判準是 selftest 的最後一行：selftest 是一支平鋪的腳本，任何未捕捉的例外都會讓它
**印不出 `N/M passed`**。所以：

| selftest 輸出 | 判定 |
|---|---|
| 有 `N/M passed`，且 fails > 0 | **killed**（測試抓到了） |
| 有 `N/M passed`，且 fails = 0 | **survived**（沒有任何測試在守這裡） |
| 沒有那一行 | **crashed**（這個變異無效，不算數，要換一個） |

crashed 跟 stale 一樣要回非零：它代表清單裡有一個條目已經不是有效的變異了。

實例（2026-07-26）：M13 原本寫的是「在既有事件那一支再蓋一次 `ingested_at`」，
注入後縮排對不上，selftest 印不出結尾那行。**那是改壞語法，不是變異。**
若沒有這條判準，它會被記成「殺掉了」，而那一格其實從來沒被測過。改寫成一個
同樣痛、但語法合法的單行替換之後，它是真的被殺。

### 坑三：一條「每次注入都會紅」的檢查，會把 kill 訊號變成常數

下一節那條「每個 `find` 在目標檔案裡剛好出現一次」的檢查，第一次接上去就出事了：
mutate.py 跑到某一條時，那一條的針腳**正被它自己換掉**，於是這條檢查必紅。
結果是**每一條變異都「被殺」**——包含四條早就知道守不住的（M01/M02/M03/M11），
全部被誤判成 killed。

一個對所有輸入都給同一個答案的訊號，資訊量是零。這跟 `return 0` 的 CI、跟
`survives: true` 填滿的清單是同一件事，只是這次它長在量測工具自己身上。

規則：注入期間，mutate.py 用環境變數 `MUTATE_IN_FLIGHT=<id>` 告訴 selftest
「這一條的針腳現在本來就不該在」，selftest 只跳過**那一個 id**，其餘十九條照數。
基準線那一輪不設這個值。CI 也不設——只有 mutate.py 設它，而且一次一個。
selftest 另有一條釘住「`MUTATE_IN_FLIGHT` 只認得清單裡的 id」，免得打錯字就
等於憑空關掉一條檢查。

## 清單記的是「量到的事實」，不是「希望的樣子」

每個條目都帶一個 `survives:` 欄位，記的是**上次實測的結果**。工具在**兩個方向**
都比對：

- 記 `survives: false`，實際存活 → **回歸**。有測試被刪掉、或碼被改得繞過了測試。
- 記 `survives: true`，實際被殺 → **記錄過期**。有人補了測試但沒更新清單。

只釘一個方向的話，把 `survives: true` 全部填上去就會永遠綠——那是把清單變成一份
許願單。理由跟覆蓋率那一節、跟 exit code 那一節一樣：**一個天天紅的 CI 跟一個
永遠綠的 CI 一樣沒有資訊**，而一份永遠對的清單什麼都沒說。

`survives: true` 的條目**必須**寫 `why`，講清楚為什麼現在守不住、以及誰在管
（通常是 BACKLOG 的某一條）。已知的缺口掛在那裡是誠實（紅線 8）；沒有理由的
`survives: true` 是掩蓋。

## 不准弄髒工作區

工具會真的改原始碼檔案。三道護欄：

1. **開跑前**：目標檔案在 `git status --porcelain` 裡有任何改動就拒跑。
   已經髒的樹出事時分不清哪些改動是誰的。
2. **每一輪**：原文存在記憶體，還原走 `try/finally`，Ctrl-C 也還得回來。
3. **收工後**：逐檔比對 sha256 與開跑前一致，不一致就大聲叫並回非零。
   還原失敗而沒人知道，就是把變異過的碼 commit 上去。

工具**不做** git 操作、不 commit、不 push。它只讀寫那幾個目標檔案。

## 這一層不保證什麼

- **不保證測試是對的。** 一條斷言了恆真條件的測試也會讓變異變紅。
  變異測試量的是「有沒有東西在守」，不是「守的東西對不對」。
- **不保證覆蓋完整。** 清單是人挑的，挑漏了不會有人知道。它回答的是
  「我列出來的這些地方守不守得住」，不是「所有地方」。所以條目要挑
  **壞掉最痛的**那些，不是最好寫的那些。
- **不取代 selftest。** selftest 每次 push 都跑、0.5 秒；變異盤點跑 N 次 selftest，
  是分開的一條工作流。selftest 只快速檢查清單的針腳還在不在（見下）。

## selftest 這邊只做一件事：快速抓清單過期

`scripts/mutate.py` 要跑幾十次 selftest，太慢，不適合掛在每次 push 上。但
「清單過期」這件事不需要跑變異就查得到——只要數一下每個 `find` 在目標檔案裡出現
幾次。所以 selftest 釘的是：

- `mutations.yaml` 讀得進來，`id` 不重複，必填欄位都在。
- **每個 `find` 在它的目標檔案裡剛好出現一次**（注入期間略過 `MUTATE_IN_FLIGHT`
  指到的那一個，理由見坑三）。
- 每個 `survives: true` 的條目都有 `why`。

這樣改碼的人在 0.5 秒內就會知道清單需要跟著改，不必等到週一那班。

## 第一輪的結果（2026-07-26）

20 條：16 被殺、4 存活。四條存活的是 M01 / M02 / M03 / M11，全部指向 BACKLOG P1
（修法躺在 `test/monitor-exit-codes` 上，還沒併進 `main`）——**修好了但沒併進
`main` 的，算沒修**。

第一輪跑出來、當場補掉的五個洞（原本 `survives: false` 卻真的活著）：

| id | 沒人守的地方 | 補在哪 |
|---|---|---|
| M09 | `health()` 的「從來沒抓到過」→ 判綠 | selftest：空 vault 必紅 + 反方向 |
| M14 | `missing_primary_evidence`（紅線 2 的執法點） | selftest 首次 import `pulse-gate.py` |
| M15 | `unsupported_heat`（紅線 4） | 同上，含撐得住就不擋的反方向 |
| M19 | 聚類的 96 小時窗口 | `belongs_to_event` 48h/120h 兩點 |
| M20 | keywords 的順序（`list(set)` 隨機） | 比對真的 `main()` 產出的 frontmatter |

M14 / M15 最值得記一筆：`pulse-gate.py` 是**唯一**決定發不發的地方（紅線 1），
而在這一輪之前 selftest 從來沒有 import 過它。把那兩行 `blockers.append(...)`
刪掉，224 條測試一條都不會紅。

## 第二輪（2026-07-26 稍晚，`fix/heat-claims-a-measurement`）

25 條：21 被殺、4 存活。存活的還是 M01 / M02 / M03 / M11，還是 BACKLOG P1，
一條都沒變——這一輪沒有補到任何新洞，因為新增的五條（M21–M25）是**跟著修法
一起寫的**，不是事後盤點出來的。它們全部當場被殺（M21 讓兩條測試紅，其餘各一條）。

新增的五條守同一件事：`heat` 在沒有任何傳播證據時不准長出數字。分五條不是為了
湊數，是因為「走回去」有五個互不相干的入口——源頭重新計算（M21）、門禁不擋
（M22）、`value` 悄悄改權重（M23）、前台把 `None` 印成 0（M24）、送 0 給敘述層
（M25）。堵住其中一個不等於堵住，而且後三個從後端看起來都還是誠實的：
`heat` 欄位是 `null`，只有畫面上或 LLM 收到的那份是謊。

這一輪也證了盤點清單的第二個用途：它是**修法有幾個出口**的檢查表。寫 M21 的
時候只想到源頭，是列到第三條才發現 `value` 的權重是一個獨立的說謊路徑。

## 第三輪（2026-07-26，`test/monitor-exit-codes` 併進 main 之後）

**25 條：25 被殺、0 存活。** 那四條掛了兩輪的 M01 / M02 / M03 / M11 一起倒了，
而且不是因為有人補了新測試——是因為那些測試**終於併進 `main`**。

這一輪最值得記的不是結果，是**清單怎麼發現自己過期的**。跑完 mutate.py 的
輸出不是「0 存活、恭喜」，是：

```
25 條：21 被殺、0 存活（清單有記）、4 要人管
要人管的：
  [stale-record] M01 …  ← 已經有測試守住了，清單該更新
```

`survives: true` 卻被殺掉會**判紅**，這是清單開頭那段「兩個方向都會被比對」
真的付現的一次。如果當初只釘 `survives: false` 那個方向（也就是把 survives
全填 true 就永遠綠的那種寫法），這四條會安安靜靜地繼續掛著「BACKLOG P1，
還沒併」的 `why`，而那句話從 merge 那一刻起就是假的。**一份不會抱怨自己過期
的盤點清單，跟沒有清單的差別只是心裡比較踏實。**

四條的 `why` 全部改成空字串並記下被殺當下的紅燈數（10 / 10 / 5 / 1）。
BACKLOG P1 同時結案。

順帶把「修好了但沒併進 `main` 的，算沒修」這句話量化了一次：這四條在
`test/monitor-exit-codes` 上被殺是 2026-07-26 中午的事，在 `main` 上被殺是
同一天傍晚。中間那幾個小時裡，`main` 的 CI 對 `return rc → return 0` 這件事
一無所知——而分支上的測試寫得再好，都不會有任何一班跑到它。

## 第四輪（2026-07-26，`fix/narrative-drops-the-fake-heat`）

**28 條：28 被殺、0 存活。** 新增 M26–M28，守敘述層那條新的擋線
（規格 `references/narrative-layer.md`）。

三條的分法跟第二輪同一個道理：**堵一個出口不等於堵住**。這次的三個出口是
判準永遠豁免（`find_heat_claims` 從此回空 list，擋線變成裝飾）、命中了照樣寫
進去（stderr 有抱怨，磁碟上還是那句謊）、拒收了但那一班回 0（沒寫進去，也沒有
人會知道）。後兩條特別值得分開列：它們都保留了看起來很負責的那一半——一個有
印錯誤訊息、一個有真的不寫入——**只是把會讓人發現的那一半拿掉了**。

這一輪還補到清單本身照不到的一個角：M26–M28 守的是 apply 這條**入口**，
而觸發這輪修法的那兩句話待在 `lenses`，是夜間鏈永遠不會跑到的地方。變異測試
問的是「這格壞了會不會有人叫」，它問不到「已經寫在磁碟上的東西對不對」。
那一半由 selftest 直接掃 `_config/narratives.yaml` 全檔負責，不是由這份清單負責。

## 第五輪（2026-07-26 傍晚，`fix/gate-keys-unmarked`）

**32 條：32 被殺、0 存活。** 新增 M29–M32，守 `gate.yaml` 的標記涵蓋檢查
（規格 `references/gate-config-status.md`）。

四條守的是同一條檢查的四種**安靜死法**：列舉回空 list（沒有 leaf 就沒有東西
違規，三條檢查同時變成永遠綠）、⚠ 不再是必要的（散文裡順口提一句「未接線」
就能把假欄位洗白）、消費者驗證退回純子字串（一句提到名字的註解就算有人在讀
它）、指名的消費者檔案不存在就當通過。第一條特別要記：**一條「找不到違規」的
檢查，跟一條「沒有東西可以違規」的檢查，輸出一模一樣。** 所以 selftest 裡另外
放了一個哨兵，直接釘住列舉到的 leaf 數量。

這一輪真正的收穫不是那四條被殺，而是 **M30 第一輪存活**。它守的那條測試我剛
寫完、也確實跑過、也是綠的——但它比對的是 `[e["path"] for e in parse(...)]`：
不管標記規則認不認得那三個字，列舉出來的 path 都是 `a.b`。**那條測試從頭到尾
沒有在測任何東西，而 322/322 全綠不會告訴你這件事。** 改成比對
`(path, unwired)` 之後才殺得掉。

也就是說這一輪至少有一條測試，是靠變異測試才發現它是空的。新寫的判準自帶
一批「判準自己的單元測試」時，這種空測試特別容易寫出來——因為手上有的是
剛寫好的函式，順手比對一個一定會成立的欄位，看起來跟真的一樣。

## 第六輪（2026-07-27，`fix/sitemap-zero-yield-is-not-silence`）

**38 條：38 被殺、0 存活。** 新增 M33–M38，守零產出診斷
（規格 `references/health-alarms.md`〈零產出不是沉默〉）。

六條分成兩組，因為這條修法有兩段各自可以獨立說謊：**判得出來**（M33–M35，
中途數字與判定）與**印得出來**（M36–M38，渲染）。判準寫得再準，渲染那一段
把兩種成因印成同一句，人看到的還是同一個「0 筆」。M38 是這一組的代表：
`side` 一律印「我們」，`code` 欄位仍然是對的、表格仍然長得很專業，只是那一欄
不再帶任何資訊。

M35 換過一次。原本寫的是「hints 命中數改成全命中」，直接改
`matched = [loc for loc, _ in entries]`——結果 selftest **crashed** 而不是紅：
命中之後 `adapt_sitemap` 會去 `safe_fetch` 那張子 sitemap，測試裡的
`https://x.test/...` 解不到 DNS 就炸了。`crashed` 不算「測試抓到了」（見坑一），
所以改成只動 `d["hint_matched"]` 這個計數，不碰抓取路徑。

**記在這裡是因為它會再犯**：任何一條會讓程式多打一次網路的變異，在沒有外網的
環境裡都會變成 crash。變異要挑**只改判斷、不改 I/O 路徑**的那一行。

## 第七輪（2026-07-27，`fix/evidence-forgets-what-it-saw`）

**42 條：42 被殺、0 存活。** 新增 M39–M42，守證據記錄的 round-trip
（規格 `references/evidence-tiers.md`〈證據記錄要留下什麼〉）。

四條守的是同一件事的四個位置：寫不進去（M39 白名單）、讀回來被頂替（M40）、
印出來假裝有（M41）、根本沒帶進來（M42）。**M40 是這一組的代表**：
它讓 `title` 變成 url，於是任何拿標題比相似度的規則照樣算得出一個數字——
只是那個數字算的是網址。**壞掉的欄位還會回答問題，是最難發現的一種。**

M39 也換過一次，跟上一輪的 M35 是同一種坑：原本它讓 selftest **crashed** 而不是
紅，因為測試寫的是 `e["title"]`——欄位被拿掉就 KeyError。**crash 的是測試不是
被測的碼**，那不算抓到。改成 `e.get("title")` 之後才是「值不見了→紅」。

規則補一條：**斷言一個欄位存在時要用 `.get()`，不要用 `[]`。** `[]` 會把
「欄位不見了」變成 crash，而 crash 在這份清單裡不算被殺。

## 第八輪（2026-07-27，`fix/translation-chain-counts-a-rewrite`）

**47 條：47 被殺、0 存活。** 新增 M43–M47，守跨語言轉載鏈
（規格 `references/evidence-tiers.md`〈evidence.translation_chain〉）。

五條對應這條規則的五種安靜死法，其中**四條是「把某個設定值退化成裝飾」**：
`enabled` 不讀、`window_hours` 不比、`entity_overlap_min` 不比、
`excluded_from` 不讀。這是刻意的分法——這條規則本身修的就是「gate.yaml 有一組
看起來像旋鈕、其實沒有人讀的數字」，如果接線之後那些數字仍然改了沒效果，
等於把同一個病搬了個家。**每一個設定值都要有一條變異證明它真的被讀。**

M44 是唯一一條反方向的：它拿掉「語言不同」那一關，於是兩家英文媒體各自的報導
會被判成互相轉載。前四條壞掉是「該擋的沒擋」，M44 壞掉是「不該擋的擋了」——
獨立性被做假到**反方向**，而且症狀是安靜的：Event 少一個聲音，沒有任何欄位
會說它被扣過。

## 第九輪（2026-07-27，`fix/backlog-status-is-hand-written`）

**50 條：50 被殺、0 存活。** 新增 M48–M50，守每班重生成的現況頁
（規格 `references/vault-pages.md`〈`_dashboards/backlog-status.md`〉）。

這一輪是**變異測試當場抓到兩條空測試**的一輪，兩條都是我自己剛寫的：

- **M50 第一輪存活。** 我釘的是「這一頁的文字裡不含 `prefix_filtered_all` 等
  判定字串」——把「印 source id」改成「印 status」，那條測試照樣通過，因為
  status 字串裡也沒有那幾個字。**一條只釘「不含什麼」的測試，通常什麼都沒釘。**
  改成直接比對「印出來的清單等於自己從 `source-runs.jsonl` 算出來的 id 清單」。
- **M49 第一輪存活。** 「內容沒變就不重寫」是 vault 頁的共同規則，而我的測試只
  跑到 `render()`——寫檔那條路徑在 `main()` 裡，一條都沒跑到。補成走真的
  `main()`：跑一輪、把 mtime 壓成 0、再跑一輪，mtime 還是 0 才算通過。
  **而且兩個方向都要釘**：只釘「第二輪沒重寫」的話，一支根本不寫檔的腳本也會通過。

兩條的共同形態跟第五輪的 M30 一樣：**新寫的判準自帶一批「判準自己的單元測試」時，
特別容易寫出空測試**——手上有的是剛寫好的函式，順手比對一個一定會成立的東西，
看起來跟真的一樣。373/373 全綠不會告訴你這件事。

## 第十輪（2026-07-27，`fix/dictionary-gaps-report-to-nowhere`）

**54 條：54 被殺、0 存活。** 新增 M51–M54，守跨天累積的字典補漏頁
（規格 `references/vault-pages.md`〈`_dashboards/dictionary-gaps.md`〉）。

四條裡有兩條守的是**同一種方向感**：規則壞掉的時候應該往嚴的那邊倒，不是往鬆的
那邊。M51 讓門檻讀不到時退回 0（設定檔壞掉 → 每個一次性雜訊都晉升）、M52 讓晉升
只看次數不看來源數（一個來源刷十次就進字典）。兩條的共同點是**壞掉之後系統會更
「熱鬧」**——多一堆候選，看起來像機制在努力工作。往鬆的方向壞掉特別難發現，
因為它不會少東西，只會多東西。

M53 守的是「項目 × 天」那個單位錯誤，`Sources/*.md` 的 `items_observed` 踩過
一次（956 行對 461 個相異項目）。同一個坑換一頁再出現，是因為
**`_corpus/<日>/` 的語意（當天看到的清單，不是當天新增的清單）不寫在資料結構裡，
只寫在文件裡**——任何新的消費者都會再踩一次。

## 第十一輪（2026-07-27，`fix/self-fetch-reports-200-whatever-happened`）

**56 條：56 被殺、0 存活。** 新增 M55–M56，守「自己抓的 adapter 要自己回報
status」（規格 `references/health-alarms.md`〈自己抓的 adapter 要自己回報 status〉）。

**M55 第一輪存活**，而它是這兩條裡唯一真正會騙人的那一條。我先寫的測試只釘到
「adapter 有把 403 寫進 diag」——那條在 `run_source` 把值丟掉、繼續寫死 200 的
情況下**照樣通過**。也就是說我測到了「有人算出了正確答案」，沒測到「有人用它」。

跟第五輪的 M30、第九輪的 M49/M50 是同一個形態，這已經是第四次：
**新寫的判準自帶測試時，最容易只釘到判準本身，漏掉消費那一端。**
補成走真的 `run_source()`（SELF_FETCH 那條路不做 robots 檢查，換掉 `safe_fetch`
就跑得動），而且兩個方向都釘：403 那班 status 要是 403，200 那班要還是 200。
## 第十二輪（2026-07-27，`feat/tracks-and-actors-notes`）

**60 條：60 被殺、0 存活。** 新增 M57–M60，守 `Tracks/` 與 `Actors/` 兩層
（規格 `references/obsidian-schema.md`）。

三條守的是**同一種安靜的少算**：Actor 名單只取字典（字典缺口永遠看不見）、
認不出主線的事件靜靜丟掉（六條線加起來少於總數，而沒有地方看得出來）、
別名認不出來就猜第一條（新線被併進舊線）。這一層的失敗不會少一個檔案、
不會多一個錯字，只會讓某幾則事件**不出現在任何一頁上**——而頁面本身看起來
完全正常。

**M57 又是 crash 不是紅**，而且又是我自己的測試造成的：斷言寫的是
`_pages["Actors/vLLM.md"]`，那一頁不產出時直接 KeyError。這條規則第十一輪才剛
寫進本檔（「斷言一個欄位存在時要用 `.get()`」），一輪之後我在**字典 key 上**
再犯一次。規則補完整：**斷言「某個東西應該存在」時，取值一律走 `.get()`——
不管那是 dict 的欄位還是 dict 的 key。** `[]` 會把「不見了」變成 crash，
而 crash 在這份清單裡不算被殺。

## 第十三輪（2026-07-27，`fix/c2-skips-in-silence`）

**64 條：64 被殺、0 存活。** 新增 M61–M64，守「榜單中文描述的覆蓋率」
（規格 `references/vault-pages.md`〈榜單的中文描述〉）。

四條裡有三條守的是**同一件事的三個位置**：把「量不到」寫成 0、把「從來沒有過」
算成 0 天、把「量不到」標成異常。三種都是**把一種缺席偽裝成另一種缺席**——
而這一格存在的唯一理由，就是要分辨那三種缺席。壞掉之後這一格照樣有字、照樣
看起來很專業，只是指向錯的方向。

**M63 第一輪存活，又是同一個形態，第五次。** 我釘的是 `next_coverage()`
（判準）——把呼叫端的 `write_desc_coverage(vault, now, None, None)` 改成
`(…, 0, 0)`，那條測試照樣通過。判準算得對，沒有人檢查呼叫端有沒有照著用。
補成走真的 `pulse-github.main()`，把 `collect()` 換成「API 全失敗」。

累計：M30（第五輪）、M49/M50（第九輪）、M55（第十一輪）、M57（第十二輪）、
M63（本輪）。**新寫的判準自帶測試時，測到「有人算出正確答案」、漏掉「有人用它」
是這份清單裡最常見的一種空測試。** 寫完判準的測試之後，下一條就該問：
呼叫端呢。

## 第十四輪（2026-07-27，`fix/hints-miss-without-showing-candidates`）

**65 條：65 被殺、0 存活。** 新增 M65，守〈零產出診斷〉的候選網址。

這一輪的來由值得記：**上一個功能上線的第一班就用上了，而且當場暴露自己缺一格。**
`src-mistral-news` 被正確判成 `hints_matched_nothing`，但那張沒命中的子 sitemap
網址沒印出來，所以「該改成什麼」還是不知道。

診斷層的失敗有兩種，這是第二種：
**判不出來**（沒有 code）與**判得出來但接不上下一步**（有 code、沒有可操作的東西）。
第一種上一輪修了，第二種要等它真的被用到才看得見——**沒有任何測試會告訴你
「這個判定讀完之後還是不知道要做什麼」**。

## 第十五輪（2026-07-27，`feat/actions-prepares-the-desc-worklist`）

**67 條：67 被殺、0 存活。** 新增 M66–M67，守待譯清單的「量不到 ≠ 沒有」。

兩條是**同一件事的兩層**：`pulse-github.py` 抓取全失敗時留下的佔位榜單不標
`measured: false`（上游），以及 `pulse-github-desc-prep.py` 不看那一格照樣寫空清單
（下游）。任一層壞掉，「榜單這一班沒生出來」就會被寫成「今晚沒有東西要翻」——
**而那正是這條翻譯鏈停擺幾十班沒人發現的形狀。**

**M67 第一輪存活，第六次同一個形態**（M30 / M49 / M50 / M55 / M57 / M63 / M67）。
這次連方向都一樣：我釘的是「下游會不會看 `measured`」，沒釘「上游會不會寫」。
第十三輪才剛寫下「寫完判準的測試之後，下一條就該問呼叫端呢」——這一輪要補一句：
**兩層之間的約定，兩層都要各有一條測試。** 只釘讀的那一端，寫的那一端可以安靜地
不再寫；只釘寫的那一端，讀的那一端可以安靜地不再讀。

## 第十六輪（2026-07-27，`feat/event-titles-in-chinese`）

**72 條：72 被殺、0 存活。** 新增 M68–M72，守 Event 標題的中文化
（規格 `references/obsidian-schema.md`〈Event 的標題有兩個〉）。

五條分三組：**譯文綁不綁原文**（M68 驗章、M69 待譯判定）、**量不到寫成沒有**
（M70，第三次同一個坑）、**顯示與保存**（M71 只印中文不印原文、M72 sticky）。

**M72 第一輪存活，第七次同一個形態**（M30/M49/M50/M55/M57/M63/M67/M72）。
這次踩的是這個 repo **已經記錄過兩次**的 sticky 欄位坑：`event_markdown()` 整份
重寫 frontmatter，沒明確帶過去的欄位會被抹掉。我把欄位加進 `Event` 物件、加進
reload、加進 `event_markdown()`——三處都寫對了，**然後測試只釘了「寫得出去」，
沒釘「第二輪之後還在」**。

而這一條漏掉的代價特別隱蔽：譯文被抹掉之後，那則 Event 會**安靜地重新排進待譯
清單**，於是潤稿端每晚翻一次、每晚被洗掉一次，翻譯覆蓋率永遠上不去，而每一晚
的摘要都會說「今晚翻了 N 條」。**一個每天都在工作、每天成果都歸零的迴圈，
從任何單日的紀錄看都是正常的。**

規則再補一句：**sticky 欄位的測試一定要跑第二輪。** 只跑一輪測到的是
「寫得出去」，那跟「留得住」是兩件事。

## 第十七輪（2026-07-27，`chore/clean-the-root`）

**75 條：75 被殺、0 存活。** 新增 M73–M75，守機械的死引用檢查（`lib/deadrefs.py`）。

這一輪的來由不是新功能，是**清根目錄的副產品**：搬兩個檔案的時候，撞見兩處 docstring
指著不存在的檔案，其中一處已經錯了很久。docstring 不會被執行，所以永遠不會有人
「踩到」——只會有下一個人照著它去找，找不到，然後以為東西被刪了。這是「警報自己把
自己關掉」的一個變種：**沒有任何機制在量這件事，所以它可以錯到天荒地老而永遠是綠的。**

**M73 第一輪存活，第八次同一個形態**（M30/M49/M50/M55/M57/M63/M67/M72/M73）。
但這一次的病灶比前八次更值得記，因為它不是「漏測了呼叫端」——**它是測試綠在錯的
理由上。**

`resolves()` 有兩關：判準 1 問「這個字串到底有沒有在指本 repo」，判準 2 才真的去
找檔案。我為判準 2 寫了一條單元測試，斷言 `scripts/pulse-probe.py` 裡的
`lib/sources.py` 解得開。它是綠的。它綠在**判準 1 提早回 True**——頂層沒有 `lib/`，
所以那個字串在第一關就被判成「不是 repo 路徑」放掉了，判準 2 那兩行根本沒被執行過。

於是：repo 裡三分之二的路徑引用都是 `lib/xxx.py` 這個形狀，整條檢查實際上只在跑
剩下那三分之一，而它報「0 條死引用」。**一個只檢查三分之一的檢查，跟一個檢查全部
的檢查，在輸出上長得一模一樣。**

單元測試抓不到，因為單元測試問的是「結論對不對」。變異測試抓得到，因為它問的是
「這行程式碼被改壞了，有沒有人會叫」——M73 把判準 2 整個拿掉，445 條測試沒有一條紅。

規則補一句：**一條測試通過的時候，要能說出它是走哪條路徑通過的。** 說不出來，
它釘住的可能是別的東西。這件事人不容易自己看出來（我當下確實以為它在測判準 2），
所以它是變異測試而不是 code review 該抓的——**這一輪是變異測試第一次抓到一個
「我以為我測了、而且我看過原始碼」的洞。**

## 第十八輪（2026-07-27，`fix/one-clock-utc-storage-taipei-display`）

**81 條：81 被殺、0 存活。** 新增 M76–M81，守「儲存 UTC、顯示台北」
（規格 `references/timezones.md`）。M07 從 `pulse-monitor.py` 搬到 `lib/clock.py`
——釘的是同一件事，位置換了。

這一輪的來由是使用者問了一句「我的系統對齊台北時間嗎」。答案是三層各自對齊不同的
東西，而**沒有人決定過**其中兩層。

**兩條第一輪存活，而且失效理由不同——值得分開記。**

**M78（`utc_today()` 退回 `date.today()`）：測試量不到，因為值一樣。**
容器與 Actions runner 都是 UTC，所以兩個寫法在這裡永遠給同一個答案。拿值去比對
釘不住任何東西。修法是**同一個瞬間、兩個相差 26 小時的本機時區**各問一次
（`Etc/GMT-14` / `Etc/GMT+12`）：UTC 的答案必須一樣，本機時區的答案必須不一樣。
規則：**要釘「它讀的是哪個時鐘」，就得讓兩個時鐘給出不同答案；在一台
兩者重合的機器上，怎麼斷言都是空的。**

**M81（時間軸分組退回儲存日）：第九次「釘了判準沒釘消費端」**
（M30/M49/M50/M55/M57/M63/M67/M72/M73/M81）。我釘了 `fmt_date` 換算對不對，
沒釘拿它去分年月的那一端。壞掉的樣子特別安靜：卡片上寫著 `2026-08-01`，
躺在「7 月」底下——**每個數字都是對的，只有它們的關係是錯的。**

第十七輪剛寫下「一條測試通過的時候，要能說出它是走哪條路徑通過的」。這一輪要補
的是另一半：**要能說出它是拿什麼跟什麼比的。** M78 比的兩邊在這台機器上是同一個
東西，所以那條斷言不管怎麼寫都會綠。

## 第十九輪（2026-07-27，`chore/schedules-anchored-to-taipei`）

**84 條：84 被殺、0 存活。** 新增 M82–M84，守排程的台北錨點
（規格 `references/timezones.md`〈排程〉）。三條第一輪全殺，沒有可記的存活。

值得記的是**這一輪的 bug 是怎麼被發現的**：不是測試抓到的，是使用者問了一句
「腳本運作時間有對齊台北時間嗎」。查下去才看到一個叫「每週一」的排程任務寫著
`0 16 * * 1`——UTC 週一 16:00 ＝ **台北週二 00:00**。

而前一天（第十八輪）才剛把時區這件事整條修過一遍，還寫了「cron 欄位寫的是 UTC，
註解裡要附台北時間」。**那條規則只講了「時」，沒講「星期」。**
於是同一個錯的第二半原封不動地留在原地——連修這個錯的那次修正都漏了它。

規則補一句：**寫下一條「要換算」的規則時，要把「換算什麼」列完。**
「時」跟「星期」是兩個獨立的欄位，記得換其中一個不代表會記得換另一個。

這一輪也順手釘住一個**成立於巧合的關係**：每日班跑在 16:00Z，於是「一班收到的
東西」正好等於「台北的一整天」，而 `_corpus/<UTC 日>/` 的目錄名也正好等於那個
台北日。改成 20:00Z 看起來完全無害，但目錄名會跟它裝的內容錯開一天，而**不會有
任何東西紅**。這種關係以前只存在於某個人腦裡；現在 M83 守著它。

## 第二十輪（2026-07-27，`fix/c4-verifier-and-model-timeline-spec`）

**110 條：110 被殺、0 存活。** 新增 M107–M111，守 C-4 驗證器裡
「我們讀不到」與「站方拒絕」之間那道牆（規格
`docs/design/2026-07-27-published-is-a-proxy.md` 的 C-4′ / C-4″）。

**這一輪的價值全部在那一條 regression 上。** 五條裡有一條第一次跑活下來：

```
[regression] M109 攔截判斷不看狀態碼
             541/541 passed（0 條紅） ← 沒有任何測試在守這裡
```

M109 把 `if status == 403 and body:` 改成 `if body:`，也就是**讓站方真正的
拒絕被洗成「沒有判決」**。我以為這一格有測試守著，測試也確實叫
「200 一律不是攔截」——但它餵進去的 body 是
`an article about how to configure an allowlist`，
而簽名字串是 `not in allowlist`。**body 裡根本沒有簽名，所以拿不拿掉狀態碼
判斷，那條測試都會綠。**

這正是這整個 PR 在講的形態，發生在測試層：**一個標籤說自己在守某件事，
內容沒有，而不會有任何東西變紅。** 差別只在於——變異盤點就是為了偵測它而存在的，
而它這次真的偵測到了。

規則補一句，跟第十八輪那句「要能說出它是拿什麼跟什麼比的」是同一條的下一半：
**寫一條「這個輸入不該觸發 X」的測試時，那個輸入必須真的滿足 X 的其他所有條件。**
只差一個條件的輸入才在測那個條件；差兩個的，測的是別的東西。

順帶記一件事：這一輪的起因不是測試，是**上一輪的結論本身是錯的**。
C-4 被回報成「已驗證、否定」，證據是 `safe_fetch` 回 403——而那 403 來自
執行容器的 egress allowlist，不是站方。M107–M111 守的就是「下次不會再靠人
記得分辨這兩件事」。

## 第二十一輪（2026-07-27，同一條分支，模型時間線純函式層）

**117 條：117 被殺、0 存活。** 新增 M112–M118，守 `lib/modelline.py`
（規格 `references/model-timeline.md`）。

**七條裡有兩條第一次跑活下來，而它們跟第二十輪那條是同一個家族。**

```
[regression] M116 網域重新被當成 model id        559/559 passed（0 條紅）
[regression] M118 unmatched 的分母改回「全部條目」 559/559 passed（0 條紅）
```

- **M116**：`model_ids_in()` 有兩道關卡——TLD 名單、必須含數字。測試只給
  `claude.com`，而它**沒有數字**，所以第二道關卡單獨就擋住了它。
  拿掉 TLD 名單，測試照樣綠。補一個 `gpt-5.app`（有數字）之後才測得到。
- **M118**：測試拿一組 **0 條 unmatched** 的資料去比對分母。
  `0/2` 與 `0/3` 都是 `0.0`——**兩條路徑在這組資料上不分岔。**
  補一組會分岔的（正確分母 1.0、錯的分母 0.33）之後才測得到。

第二十輪寫下的規則是：「寫一條『這個輸入不該觸發 X』的測試時，
那個輸入必須真的滿足 X 的其他所有條件。」這一輪補上它的另一半：

> **測兩條路徑分不分岔時，資料必須真的會分岔。**
> 用一組兩邊答案相同的輸入去區分兩個實作，測到的是零。

同一輪裡連續三次（M109、M116、M118），三次都是**測試的名字說它在守某件事、
內容沒有**。這正是這個 repo 命名的那隻病，發生在最後一道防線上——
而它三次都被變異盤點抓到，一次都沒有靠人眼。

另外值得記的是**這一輪的七條變異全部來自真頁面**，不是想像的案例：
拿 `platform.claude.com` 的 1,436,122 bytes 跑純函式層，一路撞出七個洞，
其中兩個（lifecycle 逐日判、unmatched 用全部條目當分母）是我自己在第一版
寫進去的代理指標——**在一份專門講「不要用代理指標代表事實」的規格底下。**

## 第二十二輪（2026-07-27，同一條分支，對抗性審查之後）

**125 條：125 被殺、0 存活。** 新增 M119–M126，守一位獨立審查者對著這條分支的
diff 找出來的八個洞。

**這一輪值得記的不是變異，是那八個洞的形狀完全一致：**
**守著某件事的東西自己壞掉，而畫面完全正常。**

- `og:title` 的正規表達式在屬性值的**撇號**處截斷——`Anthropic's Opus 5`
  抽成 `Anthropic`，一個更短、**仍然像標題**的字串，然後寫進那份
  「可以引用的證據」。
- 三個機器可讀日期的抽取器**從來沒被呼叫過**：測試的 fixture 裡沒有那三個
  tag，斷言的是 `None == None`。而「機器可讀日期全部 ABSENT」已經被當成
  關於世界的事實寫進設計文件。抽取器壞掉的話，「我們讀不到」就變成
  「站方沒有寫」——紅線 7 那句話的第四種犯法。
- robots 算了、存了、印了，然後**無條件往下抓**。姊妹支
  `verify-policy-sources.py:216` 一直是對的，這支在同一個 PR 裡沒有照做。
- `unavailable_403` 被讀成站方拒絕——**這整條分支在修的就是這句話。**
- `unknown` / `ambiguous` 兩個比率的母體是全部條目，而
  `unmatched_model_rate` 才剛因為**同一個理由**在第二十一輪修過
  （0.79→0.044）。同一個函式裡的另外幾格原封不動留著。

最後那一條是這一輪唯一真正的教訓：

> **修掉一個實例不等於修掉那個形態。**
> 修完 `unmatched_model_rate` 的那一刻，同一個 `rates()` 裡還有三格
> 犯著一模一樣的錯，而我剛剛才寫下為什麼那樣是錯的。

連同第二十輪的 M109、第二十一輪的 M116/M118，這條分支上一共有
**十一個**「以為守著、其實沒有」的位置，沒有一個是靠人眼發現的：
三個由變異盤點抓到，八個由對抗性審查抓到。兩道防線各自漏掉對方抓到的那些。

## 第二十三輪（2026-07-27，同一條分支，第二輪對抗性審查之後）

**132 條：132 被殺、0 存活。** 新增 M127–M133，守第二輪審查找出來的三個洞
（外加四個小的）。**三個洞全部長在第一輪的修正裡。**

兩件事值得單獨記下來。

### 一、兩個 bug 互相掩護，而測試在上面睡著

第一輪的修正加了兩樣東西：詞首的 `\b`，與「同一條裡有層級版本就清掉所有裸數字
版本」的過濾器。守它們的測試是「`claude-3-7-sonnet-20250219` 不得產生
`claude@3`」，它一直是綠的。

實測拆開看：

```
`\b` 加或不加，對那串 model id 的比對結果**一模一樣**（那裡本來就有詞界）
過濾器則把 claude@3 清掉了——測試綠的原因是它，不是 `\b`
而同一個過濾器也把「retired the Claude 2.0, Claude 2.1, …」的前兩個刪了
```

也就是說：**一個沒有作用的修正，和一個作用過頭的修正，合起來讓測試變綠。**
`\b` 的代價是字典裡 8 個中文 alias 在中文散文裡全部失效（CJK 字之間沒有詞界），
而那個代價在測試上完全看不見，因為沒有一條測試用中文。

規則補一句，接在第二十輪與第二十一輪那兩條後面：

> **一條測試綠的時候，要能說出是哪一段碼讓它綠的。**
> 兩個改動同時進場、只有一條測試，那條測試量的是它們的和，不是任何一個。

### 二、第四次「以為守著、其實沒有」，而且是在補前一次的洞時犯的

第一輪指出 `main()` 的 `max` 改 `min` 全綠。補的測試是：

```python
_vamod.VERDICT_EXIT.get("some_future_verdict", 4) == 4
```

**那在測 `dict.get` 的預設值行為，不管碼怎麼寫都會過。** M132 因此活下來。
真正的判斷躲在 `main()` 裡，沒有任何測試叫得到——所以把它拉成 `exit_code()`。

連同 M109（第二十輪）、M116/M118（第二十一輪），這條分支上一共
**四次**補一個空測試，四次都是變異盤點抓到的。

## 第二十四輪（2026-07-28，`fix/c4-verdict-outlives-the-artifact`）

**134 條：134 被殺、0 存活。** 新增 M134–M135，守 C-4 判決的留存。

**這一輪唯一值得記的，是同一個錯誤的第五次。**

C-4 跑完之後，判決只活在一個 artifact 裡：要下載才看得到、預設 90 天過期、
`git` 讀不到。而 C-4 是整個模型時間線層的閘。**把一個 gate 的答案放在一個會
過期、又接不到的地方**——跟這個 repo 一路在抓的「量到了但沒人接得到」同形態，
只是這次的介面在 CI 與人之間。修法是把判決印進 `$GITHUB_STEP_SUMMARY`。

補的測試是：

```python
"沒有判決" in _c4mod.EXIT_MEANING.get("99", "未登記的退出碼，當成沒有判決處理")
```

**那是在讀測試自己寫的預設值**，`"99"` 不在 dict 裡，所以 `get` 回的是右邊那個
字串——不管碼怎麼寫都會過。M135 因此活下來。

同一條錯誤在這幾輪的紀錄：

| 輪 | 變異 | 空在哪 |
|---|---|---|
| 二十 | M109 | 輸入不含簽名字樣，兩條路徑都不會分岔 |
| 二十一 | M116 | 兩道關卡，測試只讓其中一道有機會作用 |
| 二十一 | M118 | 兩個分母在那組資料上答案相同 |
| 二十三 | M132 | `VERDICT_EXIT.get("x", 4) == 4` —— 讀的是測試自己的預設值 |
| 二十四 | M135 | 同上，換一個 dict |

前三次的規則是「輸入要真的會分岔」。後兩次是同一個更窄的形狀，所以給它一條
自己的規矩，而且是可以機械檢查的：

> **斷言裡不准出現 `X.get(k, 預設)`。**
> 那個預設是你寫的，不是碼寫的。要測預設值行為，就去呼叫真的會用到它的那支函式。

M132 那次的修法是把判斷從 `main()` 拉成 `exit_code()`；這次是把渲染從 `main()`
拉成 `render()`。**兩次都是同一個原因：真正的判斷躲在 `main()` 裡，沒有測試
叫得到它，於是補測試的人只好去戳旁邊那個常數。**

## 第二十五輪（2026-07-28，`fix/two-lies-on-the-public-site`）

**138 條：138 被殺、0 存活。** 新增 M136–M139，守對外站上那兩句假話的修正。

四條變異第一次跑活了兩條，而**兩條的成因都是「斷言碰不到被改的那一段」**：

- **M137**：測試查的是 `".factor .bar-unmeasured{" in CSS`——把規則整個掏空成
  `{}` 之後那串字**還在**。查選擇器不等於查宣告。
- **M139**：測試給的字串是「本則只有單一獨立來源，因此保守處理。」——**沒有括號**，
  所以錨定與不錨定的兩個 regex 都不會動它。要抓那個差別，字串得是
  「（多來源佐證）這四個字是作者引用的」這種**帶括號、且不在結尾**的。

連同這條分支先前的 M109 / M116 / M118 / M132 / M135，這是第七與第八次。
把前面幾條規則收成一句可以在 review 時直接問的：

> **「這條測試如果紅了，是碼的哪一行讓它紅的？」**
> 答不出來就是空的。

三種已知的答不出來：斷言右邊由待測的碼算出來（M132/M135）、
輸入不滿足被測條件的其他前提（M109/M116/M118/M139）、
斷言只碰到被改那一段的外圍（M137）。

**另外值得記的一件不在變異清單裡**：這一輪的遷移腳本第一版寫成
`dump_frontmatter(fm) + body`，而 `parse_note()` 會吃掉 `---` 圍欄、
`dump_frontmatter()` 不會加回去——**兩支不對稱**。56 份 note 的 frontmatter
圍欄當場消失，`pulse-render` 下一秒印 `published=0`。前一支遷移
（`migrate-2026-07-27-evidence-titles.py:147`）一直是對的，我沒去看它。
沒有任何測試會抓到這件事，抓到它的是「跑完之後去看真的輸出」。

## 第二十六輪（2026-07-28，`feat/readable-site`）

**151 條：151 被殺、0 存活。** 新增 M134–M152，守站台這一輪的字級級距、
泳道、判讀列、標頭統一與導覽。

**這一輪的教訓只有一句，但它出現了八次。**

從第二十輪到這一輪，累積八次「補的測試是空的」，而八次可以歸成三個形狀：

| 形狀 | 次數 | 例 |
|---|---|---|
| 斷言的右邊由待測的碼算出來，或乾脆寫死在測試裡 | M132、M135、M152 | `VERDICT_EXIT.get("x", 4) == 4`；把 (導覽字, h1) 對照表抄進測試 |
| 輸入不滿足被測條件的其他前提 | M109、M116、M118、M139 | 「200 不是攔截」的 body 裡沒有簽名字樣 |
| 斷言只碰到被改那一段的外圍 | M137 | 查 `.bar-unmeasured{` 這串字，而規則被掏空成 `{}` 之後那串字還在 |

再加上兩次不在變異清單裡、但同源的：**在原始碼裡 grep 一個字，打中的是
我自己寫在註解／docstring 裡解釋這件事的那句話**（「零 LLM」那次、
`<style>` 那次）。兩次的修法都一樣——**不要在原始碼裡找字，去產出裡找**。

所以規則收成一句，而且它是可以在 review 時直接問出口的：

> **這條測試如果紅了，是碼的哪一行讓它紅的？**
> 答不出來就是空的。答得出來但那一行是註解，也是空的。

**另外記一件不在變異清單裡的事**：這一輪的遷移腳本第一版寫成
`dump_frontmatter(fm) + body`，而 `parse_note()` 會吃掉 `---` 圍欄、
`dump_frontmatter()` 不加回去——56 份 note 的圍欄當場消失。
抓到它的不是測試，是**跑完之後去看真的輸出**。這一輪的 GitHub 動能頁
也是同一個方法抓到的：它不經過 `pulse-render`，所以四頁都改完之後，
只有它還留著「0 LLM 判斷」、還缺 `mobile-nav`。

## 第二十七輪（2026-07-28，`fix/c2-apply-cannot-read-the-board`）

**157 條：157 被殺、0 存活。** 新增 M153–M158，守 `pulse-github-desc-apply.py`
的「英文原文從哪來」三態。

**這一輪的起點不是變異，是一次實跑。** 我先前一直寫「這條可能還壞著」，
這次改成 clone 一份 `origin/main`（`397cd57`、`--depth 1`，就是潤稿端拿到的
那一份）、照 runbook 第 11 步寫三條完全合格的譯文跑 apply：3/3 退件、
離開碼 0、退件理由印「榜換過了」——而榜一次都沒換。

那條測試從一開始就在，而且從來不會紅：

```python
_dam.validate("ghost/repo", _ZH, None)[1] == "不在目前榜單上（榜換過了…）"
```

`validate()` 只拿得到 `src_present: bool`，本來就分不出「榜上沒有這條」跟
「我讀不到榜」。壞的那一行在 `main()` 的 `else {"repos": []}`，測試碰不到它。
**第二十六輪那張表的第三個形狀（斷言只碰到被改那一段的外圍），再一次。**
修法是把測試打在 `main()` 上：建一個沒有 `dist/` 的暫存 vault，真的跑一次。

**這一輪學到一件新的，值得單獨記：崩潰會掩蓋判斷錯誤。**

M153／M154 第一次跑回報 `crashed` 而不是 `killed`。原因是 `main()` 裡寫著
`repos = board["repos"]` 與 `board["repos"] = repos`——它信任 `english_source()`
回的 `origin` 跟 `board` 一定對得上。變異把 `origin` 弄錯之後，這兩行丟
`TypeError`，selftest 整個中斷，於是**測試永遠看不到那個判斷錯在哪**，
只看得到一個 traceback。

改成 `(board or {}).get("repos") or []` 與 `dict(board or {}, repos=repos)` 之後，
錯的 origin 會**印出錯的結果**而不是炸掉，兩顆變異當場被殺。規則：

> 下游對上游的回傳做假設，假設破了要**印出錯的答案**，不要丟例外。
> 一個崩潰只告訴你「這裡爆了」，一個錯的答案才告訴你「哪個判斷錯了」。

這跟這個 repo 的老毛病是同一件事的另一面：把兩種成因壓成同一個訊號，
差別只在這次壓成的是 traceback 不是 `[]`。

## 第二十八輪（2026-07-28，`feat/newspaper-layout`）

**162 條。** 新增 M162–M164（報頭共用、空狀態的 h1、頭版的朗讀順序），
retarget M152 與 M161（它們守的東西沒變，但錨點的那幾行在報紙版型下不存在了），
**M159 退場**。

**M159 為什麼退場，值得寫清楚。** 它守的是「`lead` 這個名字被 hero 修飾詞與段落
樣式共用」。報紙版型把首頁的 hero 整個換掉之後，站上再也沒有任何元素同時帶著
`lead` 與 `shell`——**那個碰撞在現在的標記裡做不出來**。不是問題消失了，是那一種
形狀消失了。一般化的不變式（`.shell` 擁有 width 與 margin）仍然由 `_shell_conflicts`
守著，而且配了一條餵合成例子的反向測試，確保它不是永遠回空清單。
`mutations.yaml` 裡留了一段字說明它去哪了——**靜靜刪掉一條檢查，跟靜靜刪掉一個
功能一樣危險**。

### 這一輪重複了兩個已知形狀，兩個都是我自己犯的

**一、grep 打中我自己的註解。** 我在報頭那段寫了「不叫 masthead——那個字被
selftest 擋著」，而那條測試就是在原始碼裡找 `masthead`。**解釋這件事的那句話
本身觸發了那件事。** 第十次同一個形狀（前九次見第二十六輪那張表）。改法一樣：
不要在原始碼裡找字；找不到更好的判準時，至少不要在同一個檔案裡寫出那個字。

**二、崩潰蓋掉判斷錯誤。** 新加的兩條測試一條用 `str.index()`、一條用
`re.search(...).group(1)`。變異一把對應的區塊拿掉，兩者分別丟 `ValueError` 與
`AttributeError`，`selftest` 整份中斷，`mutate.py` 於是回報 `crashed` 而不是
`killed`——**測試看不到那個判斷錯在哪，只看得到一個 traceback**。

這正是同一天 c2 那一輪剛學到的那句話，同一天再犯兩次。所以它從 commit message
升級成這裡的一條規則：

> 測試在取產出裡的東西時，用 `find()` 不用 `index()`、用 `if m else` 不用
> `.group()`。**斷言要能印出一個錯的答案，不要丟例外。**

### 一條工程紀律，這輪學到的

**`mutate.py` 在跑的時候不要改檔案。** 我一邊跑整套變異一邊修 CSS，中斷時有兩個
檔案被留在「變異後」的狀態（`pulse-narrative-apply.py`、`lib/gate_keys.py`），
是 `git status` 抓到的。mutate 的流程是「讀原檔 → 寫變異 → 跑 → 寫回原檔」，
中途編輯會落在那個窗口裡。要嘛等它跑完，要嘛先 commit 再跑。

## 第二十九輪（2026-07-28，`feat/newspaper-longform`）

**164 條：163 被殺、1 存活（M160，清單有記）。** 新增 M165／M166（事件頁的原標題、
證據層級沒標時印什麼），retarget M95／M150／M152——它們守的東西都沒變，只是錨點
那幾行被長文版搬走了。

**這一輪最值得記的是一個「重複」的新位置。** 版面鑑別線搬進 `page_layout` 之後，
四頁的 `hero()` 副標就變成同一句話的第二份：鑑別線說「這一版收什麼」，副標又說
一次。同一種東西留兩份、改一邊另一邊分岔——這個 repo 抓過很多次的形狀，這次長在
**文案**上而不是程式或設定上。

抓到它的不是我，是 M150：舊的那顆守「kicker 混用中英」，長文版把英文 kicker 整組
拿掉之後它的前提消失，retarget 成「頁首把鑑別線那一句再印一次」——然後它**存活了**，
因為當下沒有任何測試在守。補上 `_deck_dupes()` 才殺掉。

> 一顆變異失去前提的時候，先問「它守的那件事現在長什麼樣」，再問「要不要留」。
> 直接刪掉的話，那件事就沒有人守了，而清單上看起來一切正常。

## 第三十輪（2026-07-29，`feat/newspaper-longform` 續）

**168 條：167 被殺、1 存活（M160，清單有記）。** 新增 M167–M170（GitHub 兩軸），
並修好上一輪自己弄壞的來源更新頁。

**這一輪的主角不是變異，是一個排名指標的偏袒。** 星速榜排的是 Δ★/天，而那個量
對大 repo 有系統性優勢：同樣一天漲 200 顆，10 萬星的專案是日常、2 千星的是暴動，
排序看不出差別。**用一個對大者有先天優勢的指標去代表「誰在竄」**——同一隻病換到
排名上。

修法**不是合成一個動能分**。合成就是再造一個代理指標，而權重要多少沒有人答得出來。
拆成兩個各自誠實的軸，並排印，把各自偏袒誰寫在頁面上：

| 軸 | 量什麼 | 偏袒誰 | 護欄 |
|---|---|---|---|
| 星速 | Δ★/天 | 大 repo | 首次觀測不給值 |
| 竄升 | Δ%/天 | 小 repo | 低於 200 顆星不列 |

順手拆掉一個舊代理：首次觀測原本拿「星數 ÷ 建立至今天數」當動能。那是**歷史平均**
不是現在的速度——三年前開的 5000 星專案會拿到 4.5★/天 排進前段，而它這一週可能
一顆都沒漲。兩點才有斜率，一點沒有，所以現在留 None、排最後、標「首次觀測」。

還有一個更小但同源的：竄升榜同分時原本用星數破，那等於**把絕對軸的偏袒偷渡回
相對軸**——而這個榜存在的理由就是不要那個偏袒。改用名字破：任意，但不偏袒任何一種
repo，而且重跑得到同一個順序。

### 又一次在 mutate 跑的時候改檔案

第二十八輪才寫下「`mutate.py` 在跑的時候不要改檔案」，這一輪又犯，`lib/gate_keys.py`
再次被留在變異狀態，一樣是 `git status` 抓到的。**寫下一條規矩不等於會遵守它**——
這句話這個 repo 已經在別的地方說過（〈已經修掉的〉裡那條靠人記得的複量機制）。
真正的修法是讓它不可能發生：mutate 開跑時鎖住工作區，或跑在一份 worktree 副本上。
記進 BACKLOG。
