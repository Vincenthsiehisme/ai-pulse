# Incident — arXiv 語料在 robots 檢查失效下取得

- **發現日期**：2026-07-24
- **處置日期**：2026-07-24
- **嚴重度**：高（紅線事件，非功能缺陷）
- **狀態**：已處置，來源永久停用

---

## 一句話

`export.arxiv.org` 的 robots.txt 為全面禁止，但 `pulse-probe.py` 的 robots 檢查因兩個疊加缺陷而從未真正生效，導致 M1 期間在未經許可的情況下取得約 100 筆語料。

---

## 事實

`export.arxiv.org/robots.txt` 內容為：

```
User-agent: *
Disallow: /
```

這與兩件事矛盾：

1. `arxiv.org` 主站的 robots 不同，主站對 `/rss` 未禁止。
2. arXiv 自身的批次取用說明頁明講「請有 harvest 需求者改用 export.arxiv.org，此站專為程式化存取而設」，並提供每日 RSS。

常見解釋是那條 `Disallow: /` 用於擋搜尋引擎索引鏡像站、非針對 API/RSS 客戶端。**但這是詮釋，不是事實**，本紀錄不採用它作為行動依據。

---

## 根因：兩個缺陷疊加

| # | 缺陷 | 效果 |
|---|---|---|
| 1 | `robots_allows()` 用 urllib 預設 UA 取 robots.txt，內容卻用本系統宣告的 UA 抓 | 檢查回答的是「另一個客戶端能不能抓」，與實際行為無關 |
| 2 | 取不到 robots 時回 `None`，呼叫端只擋 `is False` | 未知即放行。而 `robots_allows` 的 docstring 明寫「未知不當成允許」——行為與自身文件相反 |

單獨任一缺陷都可能被察覺；兩者疊加後，這條來源在整個 M1 期間從未被真正檢查過，且沒有任何日誌顯示異常。

**這是本事件真正的教訓**：失效的安全檢查與通過的安全檢查，在日誌上長得一模一樣。

---

## 處置

1. `src-arxiv-cs-cl` 標 `dormant` / `robots_ok: false`，永久停用，不去信詢問、不重議。
2. `_corpus` 中該批約 100 筆語料移除。
3. 本檔取代該批語料留存。**語料可以刪，紀錄不可以。**
4. 不採用「改抓 arxiv.org 主站 RSS」的替代路徑——守字面而違背 arXiv 明示的請求，比停用更糟。

---

## 已修正的程式碼行為

- robots.txt 改用與內容抓取相同的 UA 取用。
- 狀態碼分支明確化：
  - `200` → 依內容判定
  - `401` / `403` → 視為拒絕
  - 其餘 `4xx` → 視為允許（無 robots 檔）
  - `5xx` 與例外 → `unknown`
- `unknown` 一律不放行。`robots_ok: null` 在 `sources.yaml` 中意為「尚未確立」，不是「沒問題」。

---

## 連帶後果（非缺陷，是設計前提被推翻）

arXiv 是官方線中唯一具真實具名作者的來源。其餘來源的 author 欄位多為機構署名、handle 或發版 bot。它出局後，checklist 5b（可解析為單一自然人）的分子接近零。

因此：

- **人物層 M1 不做。** 不是 `people.yaml` 設計不好，是官方線本來就不署名。
- 5b 自 M1 驗收條件移除；5a 保留，僅作 adapter 解析失敗偵測。
- `author` 解析、`PERSON_KINDS`、`people.yaml`、`corpus_type: person_signal` 一律保留但不出報表，待 M3 KOL 線進來後復用。
- 七天觀測的目的隨之改變：從「判斷人物層值不值得建」改為「判斷字典往哪長」。

---

## 未解

- `_corpus` 全量進版控的保留策略仍未定，`seen.json` 同理會單調成長。
- 本事件顯示「檢查失效但外觀正常」是一整類風險。目前只修了 robots 這一處，尚未盤點其他同型檢查（例如 quota、lifecycle 過濾、encoding 判定）是否有相同的「未知即放行」寫法。這件事值得在正式跑之前掃一遍。
